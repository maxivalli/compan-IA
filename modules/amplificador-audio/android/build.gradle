apply plugin: 'com.android.library'
apply plugin: 'expo-module-gradle-plugin'

android {
  namespace "expo.modules.amplificadoraudio"
  defaultConfig {
    versionCode 1
    versionName "1.0.0"
  }
}

dependencies {
  compileOnly project(':expo-modules-core')
}
